{
  "name": "@lgs1920/encoder-frame-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "DEMO_OPEN_BROWSER=false bun run build && bun run server.ts",
    "build": "eleventy && bun build src/app.ts --minify --outdir dist/assets",
    "start": "bun run build && bun run server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@awesome.me/webawesome": "3.12.0",
    "@11ty/eleventy": "3.1.6",
    "typescript": "7.0.2"
  },
  "devDependencies": {
    "@types/bun": "^1.4.1"
  }
}
